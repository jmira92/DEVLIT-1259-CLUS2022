# bash/sh/zsh environment setup for ConfD confd-7.7.1
CONFD_DIR=/Users/jomira/DEVV/netsim/confd
PATH=$CONFD_DIR/bin:$PATH
if [ "x${DYLD_LIBRARY_PATH}" != "x" ]; then
    DYLD_LIBRARY_PATH=$CONFD_DIR/lib:$DYLD_LIBRARY_PATH
else
    DYLD_LIBRARY_PATH=$CONFD_DIR/lib
fi
if [ "x$PYTHONPATH" != "x" ]; then
    PYTHONPATH=$CONFD_DIR/src/confd/pyapi:$PYTHONPATH
else
    PYTHONPATH=$CONFD_DIR/src/confd/pyapi
fi
export CONFD_DIR PATH DYLD_LIBRARY_PATH PYTHONPATH
